spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/sureshsehrawatt/config-test
          default-label: qa
          clone-on-start: true
        bootstrap: true
        enabled: true

server:
  port: 7770
